function ContactSection({ id }) {
  return (
    <section id={id} className='fixed bottom-0 w-full h-[100px] flex align-end'>
      <h1 className="font-chillax font-semibold text-[8.5vw]">{`Let's chat!`}</h1>    
      
    </section>
  )
}

export default ContactSection;

